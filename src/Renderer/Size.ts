export class Size
{
    Width: number;
    Height: number;
    Length: number;

    constructor(width?: number, height?: number, length?: number)
    {
        this.Width = width;
        this.Height = height;
        this.Length = length;
    }
}